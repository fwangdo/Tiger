/* A simple Tiger program */
let
    type intArray = array of int

    var arr := intArray[10] of 0

    function fact(n: int): int =
        if n <= 1 then 1
        else n * fact(n - 1)

    var x : int := 5
in
    arr[0] := fact(x);
    x + 1
end
